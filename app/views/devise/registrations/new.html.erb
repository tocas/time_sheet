<h2>Sign up</h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :class =>'form-horizontal'}) do |f| %>
  <%= devise_error_messages! %>
  <fieldset>
    <div class="control-group">
      <%= f.label :email, :class => 'control-label', :for => 'email_input'%>
      <div class="controls">
        <%= f.email_field :email , :id => 'email_input', :class => 'input-xlarge' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :password, :class => 'control-label', :for => 'password_input'%>
      <div class="controls">
        <%= f.password_field :password , :id => 'password_input', :class => 'input-xlarge' %>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :password_confirmation, :class => 'control-label', :for => 'password_confirmation'%>
      <div class="controls">
        <%= f.password_field :password_confirmation , :id => 'password_confirmation', :class => 'input-xlarge' %>
      </div>
    </div>
    <div class="form-actions">
      <%= f.submit "Sign up", :class => 'btn btn-primary'%>
    </div>
  </fieldset>
<% end %>

<%= render :partial => "devise/shared/links" %>

